---
// COMPONENT IMPORTS
import { SITE } from "@consts";
import PostCard from "./PostCard.astro";
import Container from "./Container.astro";

const numPosts = SITE.NUM_POSTS_ON_HOMEPAGE

const { collection, index } = Astro.props;

---

<Container>
    
</Container>