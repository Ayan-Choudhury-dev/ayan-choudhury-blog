---
import Container from '@components/Container.astro';
import Link from '@components/Link.astro';
import { SITE } from '@consts';

// import tippy from 'tippy.js';
---

<header
  class="fixed left-1/2 -translate-x-1/2 w-full py-5 top-0 border-2 border-transparent duration-300 ease-in-out"
  id="main-header"
>
  <Container class="w-full max-w-2xl mx-auto transition-[max-width] duration-300 ease-in-out">
    <div class="flex flex-wrap gap-y-2 justify-between items-center" id="hero_container">
      <Link href="/" underline={false}>
        <div class="font-semibold">
          <span class="dark:text-salmon_dark text-salmon_light" id="site-name">{SITE.NAME}</span>
          <br />
          <span id="tagline" class="text-sm font-normal duration-500 ease-in-out text-slate-300">
            {SITE.TAGLINE}
          </span>
        </div>
      </Link>

      <div class="flex flex-wrap gap-1 items-center">
        <!-- Music icon hidden-->
        <button
          id="Music-link-button"
          aria-label="Music Icon"
          class="hidden group size-8 flex items-center justify-center rounded-full data-[active=true]:bg-black/10 dark:data-[active=true]:bg-white/10 data-[active=true]:cursor-default"
          data-tippy-content="Now I'm listening to"
          data-active={Astro.url.pathname === '/music'}
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="group-hover:stroke-black group-hover:dark:stroke-white transition-colors duration-300 ease-in-out"
          >
            <path d="M9 18V5l12-2v13"></path>
            <circle cx="6" cy="18" r="3"></circle>
            <circle cx="18" cy="16" r="3"></circle>
          </svg>
        </button>

        <!-- Archive icon hidden -->
        <button
          id="Blog-archive-link-button"
          aria-label="Archive Icon"
          class="hidden group size-8 flex items-center justify-center rounded-full"
          data-tippy-content="Go to Blog Archive"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="group-hover:stroke-black group-hover:dark:stroke-white transition-colors duration-300 ease-in-out"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M5.25 14.25h13.5m-13.5 0a3 3 0 0 1-3-3m3 3a3 3 0 1 0 0 6h13.5a3 3 0 1 0 0-6m-16.5-3a3 3 0 0 1 3-3h13.5a3 3 0 0 1 3 3m-19.5 0a4.5 4.5 0 0 1 .9-2.7L5.737 5.1a3.375 3.375 0 0 1 2.7-1.35h7.126c1.062 0 2.062.5 2.7 1.35l2.587 3.45a4.5 4.5 0 0 1 .9 2.7m0 0a3 3 0 0 1-3 3m0 3h.008v.008h-.008v-.008Zm0-6h.008v.008h-.008v-.008Zm-3 6h.008v.008h-.008v-.008Zm0-6h.008v.008h-.008v-.008Z"
            ></path>
          </svg>
        </button>

        <!-- Dark and light mode buttons hidden-->
        <button
          id="light-theme-button"
          aria-label="Light theme"
          class="group size-8 flex items-center justify-center rounded-full opacity-40 hidden"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="group-hover:stroke-black group-hover:dark:stroke-white transition-colors duration-300 ease-in-out"
          >
            <circle cx="12" cy="12" r="5"></circle>
            <line x1="12" y1="1" x2="12" y2="3"></line>
            <line x1="12" y1="21" x2="12" y2="23"></line>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
            <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
            <line x1="1" y1="12" x2="3" y2="12"></line>
            <line x1="21" y1="12" x2="23" y2="12"></line>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
            <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
          </svg>
        </button>

        <button
          id="dark-theme-button"
          aria-label="Dark theme"
          class="group size-8 flex items-center justify-center rounded-full hidden"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="group-hover:stroke-black group-hover:dark:stroke-white transition-colors duration-300 ease-in-out"
          >
            <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
          </svg>
        </button>

        <!-- RSS Button -->
        <!-- RSS Button -->
        <a
          href="/subscribe"
          id="rss-subscribe-link"
          aria-label="RSS Icon"
          class="group size-8 flex items-center justify-center rounded-xl w-full bg-salmon_dark/80 px-4 gap-1"
          data-tippy-content="Open RSS Feed"
        >
          <span class="text-xs font-bold text-white hidden sm:block">SUBSCRIBE</span>
          <span class="text-xs font-bold text-white sm:hidden">SUB</span>
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="15"
            height="15"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="3"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="group-hover:stroke-black stroke-white group-hover:dark:stroke-white transition-colors duration-300 ease-in-out"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M12.75 19.5v-.75a7.5 7.5 0 0 0-7.5-7.5H4.5m0-6.75h.75c7.87 0 14.25 6.38 14.25 14.25v.75M6 18.75a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0Z"
            ></path>
          </svg>
        </a>
      </div>
    </div>
  </Container>
</header>

<script>
  import tippy from 'tippy.js';
  import 'tippy.js/dist/tippy.css';

  const initializeHeader = () => {
    tippy('[data-tippy-content]');

    const SCROLL_THRESHOLD = 100;
    const SCROLL_CLASSES = [
      'top-4',
      'rounded-2xl',
      'border-white/20',
      'dark:border-white/10',
      'shadow-lg',
      'z-50',
      'py-2',
      'w-[80%]',
      'md:w-1/3',
      'sm:w-1/2',
    ];

    const header = document.getElementById('main-header');
    // Select the first element child (the Container's div) instead of relying on a specific class that might get merged away
    const container = header?.firstElementChild as HTMLElement | null;
    const tagline = document.getElementById('tagline');
    const siteName = document.getElementById('site-name');

    console.log('Header initializing:', { header, container });

    if (!header || !container) {
      console.warn('Header elements not found!');
      return;
    }

    // Check if this is first load (transitions should be suppressed)
    const isFirstLoad = document.documentElement.hasAttribute('data-first-load');

    // Function to handle scroll state
    const handleScroll = () => {
      const currentScroll = window.pageYOffset;

      if (currentScroll > SCROLL_THRESHOLD) {
        // Temporarily disable transitions if first load
        if (isFirstLoad) {
          header.style.transition = 'none';
          container.style.transition = 'none';
        }
        header?.classList.add(...SCROLL_CLASSES);
        header?.classList.remove('py-5', 'top-0', 'w-full');
        container?.classList.add('max-w-xl');
        tagline?.classList.add('hidden', 'h-0', 'overflow-hidden');
        siteName?.classList.remove('dark:text-salmon_dark', 'text-salmon_light');
        siteName?.classList.add('text-white');
        // Re-enable transitions after a frame if first load
        if (isFirstLoad) {
          requestAnimationFrame(function () {
            header.style.transition = '';
            container.style.transition = '';
          });
        }
      } else {
        // Temporarily disable transitions if first load
        if (isFirstLoad) {
          header.style.transition = 'none';
          container.style.transition = 'none';
        }
        header?.classList.remove(...SCROLL_CLASSES);
        header?.classList.add('py-5', 'top-0', 'w-full');
        container?.classList.remove('max-w-xl');
        tagline?.classList.remove('hidden', 'h-0', 'overflow-hidden');
        siteName?.classList.add('dark:text-salmon_dark', 'text-salmon_light');
        siteName?.classList.remove('text-white');
        // Re-enable transitions after a frame if first load
        if (isFirstLoad) {
          requestAnimationFrame(function () {
            header.style.transition = '';
            container.style.transition = '';
          });
        }
      }
    };

    // Check scroll position on page load
    handleScroll();

    // Add scroll event listener
    window.addEventListener('scroll', handleScroll);

    // Handle music button click
    const musicButton = document.getElementById('Music-link-button');
    musicButton?.addEventListener('click', () => {
      // Only navigate if not already on music page
      if (window.location.pathname !== '/music') {
        window.location.href = '/music';
      }
    });
  };

  // Wait for DOMContentLoaded to ensure no-entrance-transition is applied first
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initializeHeader);
  } else {
    // DOM already loaded, initialize immediately
    initializeHeader();
  }

  // Re-initialize on view transitions
  document.addEventListener('astro:page-load', initializeHeader);
</script>

<style>
  header {
    transform-origin: top center;
    border-width: 2px;
  }
</style>
